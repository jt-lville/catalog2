<%= form_for(@data_note) do |f| %>
  <% if @data_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_note.errors.count, "error") %> prohibited this data_note from being saved:</h2>

      <ul>
      <% @data_note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :string %><br />
    <%= f.date_select :string %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
