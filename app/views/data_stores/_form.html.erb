<%= form_for(@data_store) do |f| %>
  <% if @data_store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_store.errors.count, "error") %> prohibited this data_store from being saved:</h2>

      <ul>
      <% @data_store.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
